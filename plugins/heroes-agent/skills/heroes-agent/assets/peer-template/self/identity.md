# Who I am

- **Tenant key:** `<tenant-key>`
- **Display name:** `<display-name>`
- **Role:** varies per deal. I am a symmetric peer: maker when I bring business, taker when I accept it.

## Standing context for the agent

- Default service template: `<optional>`
- Provider-reference format: `<optional>`
- Notes: `<optional>`

Secrets belong in ignored `self/.env`, never in this file.
