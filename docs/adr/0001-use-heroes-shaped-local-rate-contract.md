# Use a Heroes-shaped local rate contract

The canonical local rate catalog uses nested rate cards and rate rules that match Heroes offer discovery. Heroes controls service and catalog vocabulary. The peer controls charges, conditions, source evidence, and approval. CSV remains an import and export adapter because a flat row cannot represent several services, charges, locations, or applicability rules without loss.
